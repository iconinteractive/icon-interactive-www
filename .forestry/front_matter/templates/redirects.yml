---
label: Footer
hide_body: true
fields:
- name: redirects
  type: field_group_list
  fields:
  - name: from
    type: text
    config:
      required: false
    label: from
  - name: to
    type: text
    config:
      required: false
    label: to
  config:
    min: 
    max: 
    labelField: 
  label: redirects
pages:
- static/netlify.toml
